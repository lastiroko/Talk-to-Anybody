import { DayDetailScreen } from '../../src/screens/DayDetailScreen';

describe('DayDetailScreen', () => {
  it('exports a component function', () => {
    expect(typeof DayDetailScreen).toBe('function');
  });
});
